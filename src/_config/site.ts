export type SiteConfig = typeof siteConfig;

export const siteConfig = {
NavLinks: [
    {
        title: "Home",
        href: "/",
    },
    {
        title: "Projects",
        href: "/projects",
    }
]
}